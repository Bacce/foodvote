export interface wChild {
    children: any;
}

export interface IChoice {
    user: string;
    msg: string;
}

export interface IOption {
    id: number;
    name: string;
    cat: string;
    lat: number;
    long: number;
}